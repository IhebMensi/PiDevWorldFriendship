h1>Rechercher DQL Publicite par categorie</h1>
{{ form(Form) }}
<h3>Liste des publicites</h3>
<table border="2">
    <th>Nom publicite</th>
    <th>Contenu publicite</th>
    <th>Date publicite</th>
    <th>Lien</th>
    <th>Nom catégorie</th>
    <th>image</th>
    {% for pub in pubs %}
        <tr>
            <td>{{ pub.nompublicite }}</td>
            <td>{{ pub.contenupublicte }}</td>
            <td>{{ pub.datepublicite | date('y-m-d') }}</td>
            <td>{{ pub.lien }}</td>
            <td>{{ pub.categorie.nomcategorie }}</td>
            <td> <a href="">
                    <img src="{{ asset('images/') }}{{ pub.nomimage }}" style="width: 300px;height: 150px">
                </a></td>

        </tr>
    {% endfor %}
</table>